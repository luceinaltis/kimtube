extends layouts/main

block content
    .video__player
        video(src=`/${video.fileUrl}`)
    .video__info
        a(href=routes.EDIT_VIDEO(video.id)) Edit video
        h5.video__title=video.title
        span.vide__views=video.views
        p.video__description=video.description